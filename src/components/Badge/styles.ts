import styled from "styled-components";

const StyledBadge = styled.span`
  display: flex;
  justify-content: center;
  align-items: center;

  width: 56px;
  height: 21px;

  font-family: "Inter";
  font-style: normal;
  font-weight: 400;
  font-size: 12px;
  line-height: 15px;

  background: #23c9ff;
  border-radius: 10px;
  color: white;
`;

export { StyledBadge };
